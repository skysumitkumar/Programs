#include<stdio.h>
void main()
{
    char ch,ab[10];
    //printf("the entered character is:");
    //ch=getchar();
    //printf("the entered character is:");
    //putchar(ch);
    //ch=getch();
    //printf("%c",ch);
    //ch=getche();
    //printf(" this is= %c",ch);
    gets(ab);
    printf(" this is = %s",ab);
    getch();
    return 0;
}
